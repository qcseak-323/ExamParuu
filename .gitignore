# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel
.env*
# The one exception: keys only, never values. Must come after both .env*
# rules above or it is overridden by the second one.
!.env.example

/src/generated/prisma

# Generated art review pages. Rebuilt by art/gen/contact-sheet.mjs and
# art/gen/review-artifact.mjs from the candidate PNGs beside them, and they
# embed every candidate as a data URI — sheet.html alone is ~1.5MB. The
# candidates are the artefact worth keeping; these are a view of them.
art/gen/out/**/sheet.html
art/gen/out/**/review.html
